#include <iostream>
#include <vector>
#define ericxiao ios_base::sync_with_stdio(0);cin.tie(0);
using namespace std;

const int maxN = 5226;

bool can[maxN][maxN];

int T, N, K, cnt, par[maxN];

void build(int root, int n, int k){

}

void solve(){
    cin >> N >> K;

}


int main(){
    ericxiao;
    can[1][1] = true;
    for(int n = 1; i < maxN; i++){
        for(int d = 1; d < maxN; d++){
            for(int l = 0; l <= n - l; l++){
                if(can[l][d - l])
            }
        }
    }
    cin >> T;
    while(T--){
        solve();
    }
}
