#include <iostream>
#include <string.h>
#define ericxiao ios_base::sync_with_stdio(0);cin.tie(0);
using namespace std;


int main() {
    int N, Q, l, r;
    string S;

    cin >> N >> Q >> S;

    while(Q--) {
        cin >> l >> r;
        cout << l << endl;
    }

}
