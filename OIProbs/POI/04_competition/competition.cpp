#include <iostream>
#include <vector>
#include <utility>
#define pii pair<int,int>
#define F first
#define S second
using namespace std;

const int maxN = 1e4 + 326;
vector<pii> adj[maxN], rev[maxN];

int a, b, c, d, N, M;

int main(){
	cin >> N >> M;
	for(int i = 0; i < M; i++){
		cin >> a >> b >> c >> d;
	}
}
