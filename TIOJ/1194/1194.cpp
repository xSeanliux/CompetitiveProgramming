#include <iostream>
#include <string.h>
using namespace std;

string a, b, n;

int main(){
    while(cin >> n){
        if(n == "0") return 0;
        cin >> a >> b;
        cout << a << '\n';
    }
}
