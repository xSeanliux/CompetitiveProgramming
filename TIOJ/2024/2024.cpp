#include <iostream>
#include <algorithm>
#include <queue>
using namespace std;
// By S
struct Data{
    double inGrid, inHand, step;
};

int C, N, M, K;

queue<Data> bfs;

int main(){
    cin >> c;
    for(int rr = 0; rr < c; rr++){
        cin >> N >> M >> K;
        bfs.clear();
        Data d;
        d.inGrid = N;
        d.inHand = M;
        d.step = 0;
        bfs.push(d);
        Data current;
        current = bfs.front();
        while(current.inHand = ){

        }
    }
}
