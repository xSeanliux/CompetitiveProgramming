#include <iostream>
using namespace std;

int a, b, c, d, ans = 0;

int main(){
    cin >> a >> b >> c >> d;
    a *= 60;
    a %= 360;
    b *= 60;
    b %= 360;
    c *= 60;
    c %= 360;
    d *= 60;
    d %= 360;
    //turn into degrees
    for(int hr = 0; hr < 12; hr++){
        for(int mn = 0; mn < 60; mn++){

        }
    }
}
