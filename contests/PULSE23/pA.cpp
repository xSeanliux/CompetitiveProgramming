#include <bits/stdc++.h>
#define ericxiao ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define int long long int
using namespace std;

inline void solve() {
    map<int, string> state; 
    int N;
    cin >> N;
    string s;
    int id;
    for(int i = 0; i < N; i++) {
        cin >> id >> s;
    }
}

signed main() {
    ericxiao;
    int T = 1;
    while(T--) {
        solve();
    }
}
