#include <iostream>
using namespace std;

inline void solve(){
	int ox, oy, vx, vy, ax, ay, bx, by;
	cin >> ox >> oy >> vx >> vy >> ax >> ay >> bx >> by;
}
int main(){
	int T;
	while(T--){
		solve();
	}
}
